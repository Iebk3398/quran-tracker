{
  "name": "@quran-tracker/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "db:generate": "tsx node_modules/.bin/drizzle-kit generate",
    "db:migrate": "node --env-file-if-exists=../../.env.local --env-file-if-exists=../../.env --experimental-strip-types src/migrate.ts",
    "db:reset": "node --env-file-if-exists=../../.env.local --env-file-if-exists=../../.env --experimental-strip-types src/reset.ts",
    "db:studio": "drizzle-kit studio",
    "db:seed": "node --env-file-if-exists=../../.env.local --env-file-if-exists=../../.env --experimental-strip-types src/seed/index.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@quran-tracker/types": "*",
    "dotenv": "^17.3.1",
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "tsx": "^4.19.2",
    "typescript": "*"
  }
}
